# 物资管理系统 - 前端

基于 Vue 3 + TypeScript + Pinia + Vite + Element Plus 构建的现代化物资管理系统前端应用。

## 技术栈

- ⚡ **Vue 3** - 渐进式 JavaScript 框架
- 🔷 **TypeScript** - JavaScript 的超集，提供类型安全
- 🍍 **Pinia** - Vue 官方状态管理库
- ⚡️ **Vite** - 下一代前端构建工具
- 🎨 **Element Plus** - 基于 Vue 3 的组件库
- 🚦 **Vue Router** - Vue.js 官方路由

## 功能特性

- ✅ 物资分类管理（工作室物料、衣物、杂物）
- ✅ 物资增删改查
- ✅ 照片上传与预览
- ✅ 分类筛选和搜索
- ✅ 数量管理（工作室物料支持双数量）
- ✅ 统计数据展示
- ✅ 响应式设计
- ✅ Material Design 风格

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:5173` 启动。

### 3. 构建生产版本

```bash
npm run build
```

### 4. 预览生产构建

```bash
npm run preview
```

## 项目结构

```
frontend/
├── src/
│   ├── api/              # API 接口
│   │   ├── axios.ts      # Axios 配置
│   │   └── material.ts   # 物资 API
│   ├── components/       # 公共组件
│   │   ├── MaterialCard.vue      # 物资卡片
│   │   └── MaterialDialog.vue    # 物资编辑对话框
│   ├── stores/           # Pinia 状态管理
│   │   └── material.ts   # 物资状态
│   ├── types/            # TypeScript 类型定义
│   │   └── material.ts   # 物资类型
│   ├── views/            # 页面组件
│   │   └── Home.vue      # 主页
│   ├── router/           # 路由配置
│   │   └── index.ts
│   ├── App.vue           # 根组件
│   ├── main.ts           # 入口文件
│   └── style.css         # 全局样式
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 开发说明

### 代理配置

开发环境下，API 请求会自动代理到后端服务器（`http://localhost:3000`）。

配置位置：`vite.config.ts`

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3000',
      changeOrigin: true,
    },
  },
}
```

### 环境变量

可以通过 `.env` 文件配置环境变量：

```
VITE_API_BASE_URL=http://localhost:3000/api
```

### 组件库

项目使用 Element Plus 作为 UI 组件库。所有图标已全局注册，可直接使用。

### 状态管理

使用 Pinia 进行状态管理，主要的 store：

- `useMaterialStore` - 物资管理状态

## 部署

构建生产版本后，将 `dist` 目录部署到静态文件服务器即可。

推荐部署平台：
- Nginx
- Vercel
- Netlify
- CloudFlare Pages

## 浏览器支持

支持所有现代浏览器：
- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

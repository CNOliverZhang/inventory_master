# 安装新增依赖

## 安装 canvas 用于生成图形验证码

```bash
cd backend
npm install canvas @types/canvas --save
```

## 说明

- `canvas`: 用于在Node.js中生成图形验证码图片
- `@types/canvas`: TypeScript类型定义

## 安装后启动

```bash
npm run dev
```
